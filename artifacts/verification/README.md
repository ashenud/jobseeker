# Milestone verification receipts

Store generated `milestone-NN.json` receipts here. Receipts contain command and
acceptance metadata only; never store secrets, confidential texts, full prompts,
or raw third-party responses.
