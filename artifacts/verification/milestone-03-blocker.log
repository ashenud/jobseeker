Milestone 03 strict-validation blocker
Date: 2026-07-25
Result: BLOCKED

Owner-input blocker changed:
- Identity, languages, availability, rates, budgets, geography, unpaid-test,
  workload, and duration constraints are owner-confirmed.
- HERBACORIUM is owner-confirmed completed client work with approved claims,
  restrictions, and supported service IDs.

Blocking condition:
- Command:
  docker compose --profile dev run --rm --no-deps api job-agent profile-validate
- Exit code: 3
- Safe output code: profile_configuration_invalid
- Sanitized diagnostic locations:
  profile.services.4.service_id -> string_pattern_mismatch
  manifest.records.0.supported_service_ids.1 -> string_pattern_mismatch
- Root cause: the shared identifier pattern requires the first character to be
  a letter, but the required canonical service ID is
  `3d_product_visualization`.

Stop decision:
- The milestone runner requires any nonzero gate to set BLOCKED and stop.
- No implementation commit, clean-checkout reproduction, acceptance receipt,
  reviewer GO, DONE transition, or Milestone 04 work was produced.
- The next M03 recovery turn must make the identifier grammar compatible with
  the already documented `3d_product_visualization` taxonomy, rerun all Docker
  gates from the start, and preserve the same owner-approved evidence.

The diagnostic printed only field locations and error types; it did not print
portfolio text, private values, or source-file contents.

Recovery attempt: digit-leading service identifiers
Date: 2026-07-25
Result: BLOCKED

Changed condition:
- A service-only identifier grammar now accepts
  `3d_product_visualization`.
- General claim, evidence, scoring, and KPI identifiers remain letter-led.
- Regression tests cover allowed and rejected service identifier forms.
- The Docker-boundary hook recognizes only the exact Milestone 03 gate wrapper;
  arbitrary shell wrappers remain denied.

Passing gates before the stop:
- docker compose --profile dev config --quiet
- docker compose --profile dev build api
- docker image inspect jobseeker_m03_preflight-api --format {{.Id}}
- docker compose --profile dev run --rm --no-deps api job-agent profile-validate
- docker compose --profile dev run --rm --no-deps api python scripts/validate_docs.py
- docker compose --profile dev run --rm --no-deps api python scripts/validate_milestone.py --all
- docker compose --profile dev run --rm --no-deps api python scripts/validate_codex_controls.py
- docker compose --profile dev run --rm --no-deps api ruff check .

New blocking condition:
- Command:
  docker compose --profile dev run --rm --no-deps api mypy src
- Exit code: 1
- Error:
  src/job_agent/profile/models.py:509: Incompatible types in assignment
  (expression has type "EvidenceRecord | None", variable has type
  "EvidenceRecord")
- Root cause: `record` is first inferred as `EvidenceRecord` in the manifest
  loop, then reused for `evidence_by_id.get(...)`, whose result is optional.

Stop decision:
- The runner stopped before pytest and pre-commit.
- No implementation commit, main evidence capture, clean reproduction,
  independent review, acceptance receipt, DONE transition, or Milestone 04 work
  was produced.
- The next recovery turn must use a distinct optional lookup variable, rerun the
  complete Docker gate sequence from the start, and generate fresh evidence.

Machine capture:
- `artifacts/verification/milestone-03-preflight/manifest.tsv`
- `artifacts/verification/milestone-03-preflight/metadata.tsv`
- `artifacts/verification/milestone-03-preflight/logs/09-mypy.log`

Recovery attempt: typed validator and executable KPI contracts
Date: 2026-07-25
Result: BLOCKED

Changed conditions:
- The optional evidence lookup now uses a distinct typed variable.
- KPI definitions expose and execute strict count, ratio, percentage, and median
  operations with bounded numeric and zero-denominator behavior.
- Authoritative main and clean evidence runs reject a dirty source tree before
  recording a commit or running Docker gates; only an explicit preflight label
  permits the recovery tree.
- The previously failing mypy gate now passes.

Passing gates before the stop:
- docker compose --profile dev config --quiet
- docker compose --profile dev build api
- docker image inspect jobseeker_m03_preflight-api --format {{.Id}}
- docker compose --profile dev run --rm --no-deps api job-agent profile-validate
- docker compose --profile dev run --rm --no-deps api python scripts/validate_docs.py
- docker compose --profile dev run --rm --no-deps api python scripts/validate_milestone.py --all
- docker compose --profile dev run --rm --no-deps api python scripts/validate_codex_controls.py
- docker compose --profile dev run --rm --no-deps api ruff check .
- docker compose --profile dev run --rm --no-deps api mypy src

New blocking condition:
- Command:
  docker compose --profile dev run --rm --no-deps api pytest -q
- Exit code: 1
- Result before failure: 188 passed, 1 failed
- Failing test:
  tests/test_profile.py::test_private_inputs_are_ignored_and_only_scrubbed_metadata_is_tracked
- Root cause: the test invokes `git check-ignore` inside the API container, but
  Git rejects the bind-mounted `/app` repository as dubious ownership because
  this pytest gate does not configure `/app` as a safe directory.

Stop decision:
- The runner stopped before pre-commit.
- No implementation commit, authoritative main capture, clean-checkout
  reproduction, independent review, acceptance receipt, DONE transition, or
  Milestone 04 work was produced.
- The next recovery turn must make the Git-based privacy assertions safe and
  deterministic inside the container, then rerun the complete gate sequence from
  gate 01.

Machine capture:
- `artifacts/verification/milestone-03-preflight/manifest.tsv`
- `artifacts/verification/milestone-03-preflight/metadata.tsv`
- `artifacts/verification/milestone-03-preflight/logs/10-pytest.log`

Recovery resolution: exact-path Git trust
Date: 2026-07-25
Result: RESOLVED

Resolution:
- Both Git-based privacy assertions now apply command-local `safe.directory`
  trust to the exact resolved repository root and run with that root as `cwd`.
- No wildcard trust or persistent/global Git configuration is used.
- The complete Milestone 03 Docker preflight was rerun from gate 01.

Passing result:
- All eleven wrapper gates passed.
- `docker compose --profile dev run --rm --no-deps api pytest -q` reported
  189 passed.
- The pre-commit gate passed documentation, Codex controls, Ruff, mypy, and
  pytest.

Machine capture:
- `artifacts/verification/milestone-03-preflight/manifest.tsv`
- `artifacts/verification/milestone-03-preflight/metadata.tsv`
- `artifacts/verification/milestone-03-preflight/logs/10-pytest.log`
- `artifacts/verification/milestone-03-preflight/logs/11-pre-commit.log`
